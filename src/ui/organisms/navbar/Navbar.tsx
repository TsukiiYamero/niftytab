import SearchBar from "@/ui/molecules/SearchBar/SearchBar"

type Props = {}

const Navbar = (props: Props) => {
    return (
        <div>
            <SearchBar />
        </div>
    )
}

export default Navbar