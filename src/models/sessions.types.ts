export type SessionSupabase = {
    browser_name: string,
    id: number;
}

export type SessionNifty = {
    browserName: string
    id: number
}

export type SessionNiftyCount = {
    browserName: string
    id: number
    badgeContent: number;
}
