import { Settings } from '@/ui/molecules/Profile';
import { Box } from '@mui/material';

export const SettingsTemplate = () => {
    return (
        <Box>
            <Settings />
        </Box>
    );
};
